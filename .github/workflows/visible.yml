name: worker

on:
  push:

jobs:
  resolution:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        sign: ["generation", "comparison", "familiar", "slit", "mist"]
        comb: ["statement", "interpret", "fussy", "operate"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar FO
